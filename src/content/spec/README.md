# spec

Store special pages in Markdown.

Common files:
- `about.md`: About page content.
- `friends.md`: Friends/links page content.

Keep filenames consistent with routes/components that consume them.
