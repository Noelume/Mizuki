# data

Store structured data files in TypeScript (`.ts`).

Typical files:
- `anime.ts`
- `projects.ts`
- `skills.ts`
- `timeline.ts`
- `diary.ts`
- `devices.ts`
- `friends.ts`

Use exported arrays/objects matching the types used in `src/types` and current `src/data/*` modules.
